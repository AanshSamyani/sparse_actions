# Force LF for shell scripts so they run on the Linux server regardless of the OS
# they were committed from.
*.sh    text eol=lf
*.py    text eol=lf
*.yaml  text eol=lf
*.toml  text eol=lf
*.md    text eol=lf
